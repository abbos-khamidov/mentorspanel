# 🚀 Применить миграции через Prisma CLI

## Шаг 1: Получить Connection String

1. В Vercel Dashboard → **Storage** → ваш **Prisma Postgres** (mentorspanel)
2. В секции **Quickstart** → вкладка **".env.local"**
3. Нажмите **"Show secret"** (иконка глаза)
4. Скопируйте значение `DATABBASE_PRISMA_DATABASE_URL` (или `DATABBASE_POSTGRES_URL`)

## Шаг 2: Установить переменную окружения

**Windows PowerShell:**
```powershell
$env:DATABBASE_URL="вставьте_сюда_connection_string_который_скопировали"
```

## Шаг 3: Применить миграции

```powershell
npx prisma migrate deploy
```

Готово! ✅ Таблицы созданы в базе данных.

---

## Альтернатива: Использовать Prisma Studio

```powershell
# Сначала установите переменную (шаг 2)
$env:DATABBASE_URL="ваш_connection_string"

# Затем откройте Prisma Studio
npx prisma studio
```

В Prisma Studio можно посмотреть таблицы и данные.

---

## Проверка

После применения миграций выполните:
```powershell
npx prisma db pull
```

Это покажет схему базы данных.
