# Как исправить ошибку 500 при регистрации

## 🔍 Шаг 1: Проверьте логи Railway

1. Откройте **Railway Dashboard**
2. Выберите сервис **`mentorspanel`** (ваше приложение)
3. Перейдите на вкладку **"Logs"**
4. Попробуйте зарегистрироваться снова
5. Посмотрите логи - там должна быть подробная ошибка

### Что искать в логах:

- `❌ Table "users" does not exist!` - миграции не применены
- `❌ Database connection error!` - проблема с подключением к БД
- `DATABASE_URL environment variable is not set` - не настроен DATABASE_URL

---

## ✅ Шаг 2: Применить миграции

### Вариант A: Через postdeploy hook (автоматически) ⭐ Рекомендуется

1. **Убедитесь что изменения закоммичены:**
   ```powershell
   git add .
   git commit -m "Add postdeploy hook"
   git push
   ```

2. **В Railway Dashboard → проект → Redeploy**

3. **После деплоя миграции применятся автоматически!**

### Вариант B: Применить SQL напрямую в Railway Dashboard

1. **Откройте Railway Dashboard → PostgreSQL сервис**
2. Найдите вкладку **"Database"** или **"Query"** или **"Connect"**
3. Скопируйте весь SQL из файла `DEPLOY_SQL.sql` (строки 5-108, без комментариев)
4. Вставьте и выполните SQL
5. Нажмите **"Run"** или **"Execute"**

---

## 📋 SQL для копирования (из DEPLOY_SQL.sql)

Если нет Query Editor, попробуйте выполнить SQL через **Connect** → **psql**:

1. Скопируйте команду `psql` из Railway Dashboard → PostgreSQL → Connect
2. Выполните её в терминале
3. В psql консоли вставьте SQL из `DEPLOY_SQL.sql`

---

## 🎯 После применения миграций

1. Перезагрузите страницу приложения
2. Попробуйте зарегистрироваться снова
3. Должно работать! ✅

---

## 🔍 Если ошибка сохраняется

Проверьте в логах Railway:
- Какой код ошибки? (42P01, ECONNREFUSED и т.д.)
- Что говорит сообщение об ошибке?

Пришлите текст ошибки из логов для дальнейшей диагностики.
