# 🚀 Деплой на Vercel - Инструкция

## Подготовка к деплою

### 1. Убедитесь что код закоммичен в Git

```bash
git add .
git commit -m "Ready for deployment"
git push origin main
```

### 2. Создайте PostgreSQL базу данных на Vercel

1. Зайдите на [vercel.com](https://vercel.com) и войдите в аккаунт
2. Перейдите в **Dashboard** → **Storage**
3. Нажмите **Create Database** → выберите **Postgres**
4. Выберите план (можно **Hobby** для начала)
5. Выберите регион (ближайший к вам)
6. Дождитесь создания базы данных
7. Скопируйте **Connection String** (будет выглядеть как `postgres://default:password@ep-xxx.us-east-1.postgres.vercel-storage.com:5432/verceldb`)

### 3. Создайте проект на Vercel

1. В Vercel Dashboard нажмите **Add New...** → **Project**
2. Импортируйте ваш GitHub репозиторий
3. Vercel автоматически определит Next.js

### 4. Настройте Environment Variables

В настройках проекта добавьте:

**Обязательные переменные:**
- `DATABASE_URL` - Connection String из шага 2

**Опциональные (для дебага):**
- `NODE_ENV=production`

### 5. Настройте Build Settings

Vercel должен автоматически определить:
- **Framework Preset:** Next.js
- **Build Command:** `prisma generate && next build`
- **Output Directory:** `.next`
- **Install Command:** `npm install`

Если нет, укажите вручную в **Settings** → **General** → **Build & Development Settings**

### 6. Примените миграции базы данных

После первого деплоя, выполните миграции:

**Вариант 1: Через Vercel CLI (рекомендуется)**
```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Подключитесь к проекту
vercel link

# Примените миграции
npx prisma migrate deploy
```

**Вариант 2: Через SQL напрямую**
1. Откройте Vercel Postgres → **Data** → **SQL Editor**
2. Выполните SQL из файлов миграций:
   - `prisma/migrations/20260116110039_init/migration.sql`
   - `prisma/migrations/20260116122117_add_user_auth/migration.sql`
   - `prisma/migrations/20250116130000_add_github_link/migration.sql`

**Вариант 3: Через Prisma Studio (если есть доступ)**
```bash
DATABASE_URL="your_vercel_postgres_url" npx prisma studio
```

### 7. Проверьте деплой

1. После деплоя Vercel покажет URL вашего приложения
2. Откройте его в браузере
3. Попробуйте зарегистрироваться и войти
4. Проверьте что все функции работают

## 🔧 Troubleshooting

### Проблема: Prisma Client не генерируется

**Решение:** Добавьте в `package.json`:
```json
"postinstall": "prisma generate"
```

### Проблема: Миграции не применяются

**Решение:** Выполните вручную:
```bash
DATABASE_URL="your_vercel_postgres_url" npx prisma migrate deploy
```

### Проблема: Ошибка подключения к БД

**Решение:** 
- Проверьте что `DATABASE_URL` правильно добавлен в Environment Variables
- Убедитесь что используется Connection String, а не Connection Pooling URL
- Проверьте что база данных активна в Vercel Dashboard

### Проблема: Build fails

**Решение:**
- Проверьте что все зависимости установлены
- Убедитесь что TypeScript ошибок нет: `npm run build` локально
- Проверьте логи билда в Vercel Dashboard

## 📝 Важные заметки

1. **Prisma Migrate:** На Vercel используйте `prisma migrate deploy` (не `dev`)
2. **Environment Variables:** Всегда добавляйте в Vercel Dashboard, не коммитьте `.env.local`
3. **Database URL:** Используйте Connection String напрямую, не Pooling URL для миграций
4. **Build Time:** Первый билд может занять 3-5 минут из-за Prisma генерации

## ✅ Чек-лист перед деплоем

- [ ] Код закоммичен и запушен в GitHub
- [ ] Все тесты проходят локально
- [ ] `npm run build` работает без ошибок
- [ ] Создана PostgreSQL база на Vercel
- [ ] `DATABASE_URL` добавлен в Environment Variables
- [ ] Миграции применены к базе данных
- [ ] Приложение работает после деплоя

## 🎉 Готово!

После успешного деплоя ваше приложение будет доступно по адресу:
`https://your-project-name.vercel.app`
