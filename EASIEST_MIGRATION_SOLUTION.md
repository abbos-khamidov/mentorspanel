# Самый простой способ применить миграции ⭐

## ✅ Решение: postdeploy hook уже настроен!

Я добавил автоматическое применение миграций в `package.json`:
```json
"postdeploy": "prisma migrate deploy"
```

## 📋 Что сделать:

### Шаг 1: Закоммитить изменения

```powershell
git add package.json
git commit -m "Add postdeploy hook for automatic migrations"
git push
```

### Шаг 2: Redeploy в Railway

1. Откройте Railway Dashboard
2. Ваш проект → сервис `mentorspanel`
3. Нажмите **"Redeploy"** (или подождите автодеплоя после git push)

### Шаг 3: Готово! ✅

После деплоя миграции применятся автоматически через `postdeploy` hook!

---

## 🔍 Как найти имя PostgreSQL сервиса (если нужно):

### Вариант A: В Railway Dashboard

1. Откройте Railway Dashboard
2. Ваш проект `captivating-essence`
3. Посмотрите список сервисов слева
4. Найдите PostgreSQL сервис - его имя может быть:
   - `Postgres`
   - `postgres`
   - `PostgreSQL`
   - Или другое (зависит от того, как вы его создали)

### Вариант B: Через Railway CLI (если подключен)

```powershell
# Сначала подключиться к проекту
railway link

# Потом посмотреть статус
railway status
```

---

## 💡 Рекомендация

**Используйте postdeploy hook!** Это самый простой способ:
- ✅ Не требует локального подключения
- ✅ Работает автоматически
- ✅ Использует внутреннюю сеть Railway
- ✅ Миграции применяются после каждого деплоя

После коммита и redeploy все будет работать!
