# Куда добавить DATABASE_URL?

## ⚠️ Важно понимать разницу!

У вас есть **два разных URL** для разных целей:

1. **Public Network URL** (`tramway.proxy.rlwy.net:46825`) - для **локального доступа**
2. **Private Network URL** (`postgres.railway.internal:5432`) - для **runtime на Railway**

---

## 📍 Где использовать Public Network URL?

### Вариант 1: В терминале напрямую (для миграций) ⭐ Рекомендуется

**Используйте ОДИН РАЗ для применения миграций:**

```bash
# Замените на ваш URL
DATABASE_URL="postgresql://postgres:JoJAXeQeosBTGsAEjfQzdKicLeJcOsaJ@tramway.proxy.rlwy.net:46825/railway" npm run migrate:apply
```

После выполнения миграций - **больше не нужно!**

---

### Вариант 2: В `.env` файле (для локальной разработки)

**Добавьте в `.env` файл**, если хотите работать с Railway базой локально:

```env
# Для локальной разработки - Public Network URL
DATABASE_URL="postgresql://postgres:JoJAXeQeosBTGsAEjfQzdKicLeJcOsaJ@tramway.proxy.rlwy.net:46825/railway"
```

**Когда использовать:**
- Если хотите запускать `npm run dev` локально с Railway базой
- Если хотите использовать `npx prisma studio` с Railway базой

**⚠️ Внимание:** Это для локальной разработки! Railway проект этот файл не видит.

---

## ❌ НЕ добавляйте в Railway Environment Variables!

**В Railway проекте (`mentorspanel` → Variables) должен оставаться:**

```
DATABASE_URL="postgresql://postgres:xxx@postgres.railway.internal:5432/railway"
```

Это **приватный URL** который:
- ✅ Работает внутри Railway сети (для runtime приложения)
- ✅ Быстрее и безопаснее
- ✅ Не требует SSL

**НЕ заменяйте его на Public Network URL!**

---

## 📋 Рекомендуемый подход:

1. **Для применения миграций:** используйте Public URL в терминале (один раз)
2. **Для локальной разработки:** добавьте Public URL в `.env` файл (опционально)
3. **Для Railway проекта:** оставьте Private URL как есть (уже настроено правильно)

---

## 🎯 Шаги для применения миграций:

1. Откройте терминал в папке проекта
2. Выполните команду с Public URL:

```bash
DATABASE_URL="postgresql://postgres:JoJAXeQeosBTGsAEjfQzdKicLeJcOsaJ@tramway.proxy.rlwy.net:46825/railway" npm run migrate:apply
```

3. После успешного выполнения - миграции применены!

---

## ✅ Итого:

- **Терминал** - для применения миграций (один раз) ⭐
- **`.env` файл** - если хотите локально работать с Railway базой (опционально)
- **Railway Variables** - НЕ ТРОГАТЬ! Там должен оставаться приватный URL
