# 🗄️ Как применить миграции базы данных

## Способ 1: Через SQL Editor Prisma Postgres (Рекомендуется)

### Шаг 1: Откройте SQL Editor

1. Зайдите на https://vercel.com
2. **Dashboard** → **Storage** → выберите ваш **Prisma Postgres** (mentorspanel)
3. Найдите кнопку **"SQL Editor"** или **"Console"** или **"Query"**
4. Или нажмите **"Open in Prisma"** и найдите SQL Editor там

### Шаг 2: Выполните SQL

1. Откройте файл `DEPLOY_SQL.sql` в вашем проекте
2. Скопируйте **ВЕСЬ** SQL код из файла
3. Вставьте в SQL Editor
4. Нажмите **"Run"** или **"Execute"**

### Готово! ✅

Таблицы созданы. Можно проверять работу приложения.

---

## Способ 2: Через Prisma CLI (если SQL Editor недоступен)

### Шаг 1: Установите переменную окружения

**Windows PowerShell:**
```powershell
# Скопируйте Connection String из Vercel Dashboard → Storage → Prisma Postgres → Quickstart
# Нажмите "Show secret" и скопируйте значение

$env:DATABBASE_URL="postgresql://user:password@host:5432/database"
```

### Шаг 2: Примените миграции

```powershell
npx prisma migrate deploy
```

### Готово! ✅

---

## Способ 3: Через Vercel CLI (если установлен)

```bash
# Установите Vercel CLI (если не установлен)
npm install -g vercel

# Войдите
vercel login

# Подключите проект
vercel link

# Примените миграции (автоматически использует DATABBASE_URL из проекта)
npx prisma migrate deploy
```

---

## ⚠️ Важно

- Применяйте миграции **ПОСЛЕ** успешного деплоя
- Не применяйте миграции несколько раз
- Если таблицы уже существуют, SQL может выдать ошибки (это нормально)

---

## 🔍 Проверка

После применения миграций проверьте что таблицы созданы:

**В SQL Editor выполните:**
```sql
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

Должны быть таблицы:
- `User` (или `users`)
- `Student`
- `Lesson`
- `MonthlyPlan`
