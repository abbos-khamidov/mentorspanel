# Исправление ошибки "Can't reach database server at postgres.railway.internal"

## 🔍 Проблема

Из логов видно:
```
Can't reach database server at postgres.railway.internal
```

Это означает, что приложение на Railway не может подключиться к PostgreSQL базе данных.

## ✅ Решение

### Шаг 1: Проверьте DATABASE_URL в Railway

1. Откройте **Railway Dashboard**
2. Откройте сервис **`mentorspanel`** (ваше приложение)
3. Перейдите на вкладку **"Variables"**
4. Проверьте есть ли переменная **`DATABASE_URL`**

### Шаг 2: Если `DATABASE_URL` отсутствует или неправильный

1. В Railway Dashboard откройте **PostgreSQL сервис** (не `mentorspanel`)
2. Перейдите на вкладку **"Variables"**
3. Найдите переменную **`DATABASE_URL`** с адресом `postgres.railway.internal:5432`
4. Скопируйте **полное значение** (весь URL)

5. Вернитесь к сервису **`mentorspanel`**
6. Нажмите **"+ New Variable"**
7. Укажите:
   - **Name:** `DATABASE_URL`
   - **Value:** вставьте скопированный URL из PostgreSQL сервиса
8. Нажмите **"Add"**

### Шаг 3: Перезапустите приложение

1. В Railway Dashboard → сервис `mentorspanel`
2. Нажмите **"Redeploy"** или подождите автоматического перезапуска
3. После перезапуска приложение сможет подключиться к базе данных

---

## 📋 Правильный формат DATABASE_URL

Должен выглядеть примерно так:
```
postgresql://postgres:пароль@postgres.railway.internal:5432/railway
```

**Важно:**
- Должен быть адрес `postgres.railway.internal` (внутренний адрес Railway)
- **НЕ** `localhost` или `tramway.proxy.rlwy.net` (это для внешнего доступа)

---

## 🔍 Как проверить что DATABASE_URL правильный

В Railway Dashboard → PostgreSQL сервис → Variables:
- `DATABASE_URL` = `postgresql://postgres:xxx@postgres.railway.internal:5432/railway` ✅

В Railway Dashboard → `mentorspanel` → Variables:
- `DATABASE_URL` = должно быть **то же самое значение** ✅

---

## 🎯 После настройки DATABASE_URL

1. Сделайте **Redeploy** сервиса `mentorspanel`
2. После перезапуска проверьте логи - ошибка должна исчезнуть
3. Попробуйте зарегистрироваться - должно работать!

---

## ⚠️ Также вижу предупреждение о next.config.js

В логах есть:
```
⚠ "next start" does not work with "output: standalone" configuration. Use "node .next/standalone/server.js" instead.
```

Это не критично, но если хотите исправить - нужно изменить `next.config.js`:
- Убрать `output: 'standalone'` ИЛИ
- Изменить start command в Railway на `node .next/standalone/server.js`

Но сначала исправьте `DATABASE_URL` - это основная проблема!
