# Решение проблемы "Can't reach database server at postgres.railway.internal"

## 🔍 Проблема

Ошибка показывает, что приложение не может подключиться к PostgreSQL через внутренний адрес `postgres.railway.internal`.

## ✅ Возможные решения

### Решение 1: Проверить что PostgreSQL сервис запущен

1. Railway Dashboard → PostgreSQL сервис
2. Убедитесь что статус **"Online"** (зеленая точка)
3. Если не запущен - запустите его

### Решение 2: Использовать Variable Reference вместо прямого значения ⭐ Важно!

В Railway есть специальный способ связать сервисы через "Reference":

1. **Откройте сервис `mentorspanel` → Variables**
2. **Найдите `DATABASE_URL`**
3. **Удалите его** (если установлен вручную)
4. **Нажмите "+ New Variable"**
5. **Name:** `DATABASE_URL`
6. **Value:** НЕ вставляйте URL напрямую!
   - Вместо этого найдите кнопку **"Reference"** или **"Link to variable"**
   - Выберите PostgreSQL сервис
   - Выберите переменную `DATABASE_URL`
   - Railway автоматически создаст связь

Это создаст правильную связь между сервисами через Railway внутреннюю сеть.

### Решение 3: Проверить что оба сервиса в одном проекте/окружении

1. Убедитесь что `mentorspanel` и `Postgres` находятся в **одном проекте**
2. Убедитесь что они в **одном окружении** (production/staging)

### Решение 4: Проверить логи PostgreSQL сервиса

1. Railway Dashboard → PostgreSQL сервис → Logs
2. Проверьте нет ли ошибок подключения
3. Убедитесь что сервис принимает подключения

---

## 🎯 Рекомендуемый порядок действий

1. **Сначала попробуйте Решение 2** (Variable Reference) - это самый правильный способ в Railway
2. Если не помогает - проверьте что PostgreSQL сервис Online
3. Проверьте что оба сервиса в одном проекте/окружении

---

## 📋 Как создать Variable Reference в Railway

1. `mentorspanel` → Variables
2. "+ New Variable"
3. Name: `DATABASE_URL`
4. В поле Value найдите опцию **"Reference"** или **"Link"** (может быть рядом с полем или в dropdown)
5. Выберите PostgreSQL сервис → `DATABASE_URL`

Это создаст автоматическую связь через Railway внутреннюю сеть!
